# Local credentials — never commit
.env

# OS / editor cruft
.DS_Store

# Wrangler local state
.wrangler/

# Personal Claude Code overrides (project-shared settings.json is committed)
.claude/settings.local.json
# GIS pipeline downloads (DEM/imagery tiles) — regenerable, never commit
tools/gis/cache/
